name: 'covid data analysis'
description: 'Calculate average and rolling window info from JH covid data.'
# inputs:
#   who-to-greet:  # id of input
#     description: 'Who to greet'
#     required: true
#     default: 'World'
# outputs:
#   time: # id of output
#     description: 'The time we greeted you'
runs:
  using: 'docker'
  image: 'Dockerfile'
  volumes:
    - $GITHUB_WORKSPACE:/data